name: deploy-vercel-production

# DISABLED: TDG production origin is the Mozas VPS.
# NEVER deploy TDG through Vercel. Canonical path:
#   Cursor Cloud Agent → bash scripts/deploy-production.sh → SSH → VPS

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Vercel production deploy is forbidden
        run: |
          echo "BLOCKED: TDG production deploy via Vercel is forbidden."
          echo "Use Cursor Cloud Agent secrets and: bash scripts/deploy-production.sh"
          exit 2
