name: deploy-vercel-production

# DISABLED: TDG production origin is the Mozas VPS (thedigitalgifter.com / www).
# Keep this file so existing Vercel deployments remain as rollback until the
# founder confirms a live payment. Do not auto-publish to Vercel.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Vercel production deploy is disabled
        run: |
          echo "TDG production is Mozas VPS. This workflow does not deploy."
          echo "Existing Vercel deployments are rollback-only until live payment is confirmed."
