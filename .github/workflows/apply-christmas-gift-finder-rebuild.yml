name: apply-christmas-gift-finder-rebuild

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "supabase/migrations/20260909150000_christmas_gift_finder_rebuild.sql"
      - "supabase/functions/christmas-wishlist-funnel/**"
      - "supabase/functions/_shared/christmas/giftFinder.ts"
      - "supabase/functions/_shared/christmas/giftTaxonomy.ts"
      - "scripts/apply-christmas-gift-finder-rebuild.sh"
      - ".github/workflows/apply-christmas-gift-finder-rebuild.yml"

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: Production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Require Supabase access
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN || secrets.SUPABASE_TOKEN || secrets.TDG_SUPABASE_ACCESS_TOKEN }}
        run: |
          if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
            echo "::error::SUPABASE_ACCESS_TOKEN is missing in the Production environment."
            exit 2
          fi
          echo "Supabase access token present (value not printed)."

      - name: Apply gift finder migration + deploy edge
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN || secrets.SUPABASE_TOKEN || secrets.TDG_SUPABASE_ACCESS_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          SUPABASE_PROJECT_REF: kjlsocejpmnzhhduyumy
        run: |
          chmod +x scripts/apply-christmas-gift-finder-rebuild.sh
          bash scripts/apply-christmas-gift-finder-rebuild.sh
