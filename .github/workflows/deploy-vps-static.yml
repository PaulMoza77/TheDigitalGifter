name: deploy-vps-mozas

# DISABLED: TDG production deploy MUST NOT run through GitHub Actions.
# Canonical path: Cursor Cloud Agent → bash scripts/deploy-production.sh → SSH → VPS.
# Kept as workflow_dispatch stub so historical links do not revive push-based deploys.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: GitHub Actions VPS deploy is forbidden
        run: |
          echo "BLOCKED: TDG production deploy via GitHub Actions is forbidden."
          echo "Use Cursor Cloud Agent secrets and: bash scripts/deploy-production.sh"
          echo "If deploy fails, debug SSH/VPS. Do not switch providers."
          exit 2
